[package]
name = "pdf-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
tracing = "0.1"

[dev-dependencies]
tempfile = "3"

[features]
# Enable real MuPDF bindings. Requires the MuPDF native library to be installed.
mupdf = []
